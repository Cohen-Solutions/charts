{{- if .Values.obcStorageClass.enabled }}
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ include "odf-s3-bucket.obcStorageClassName" . }}
  labels:
    app.kubernetes.io/name: {{ include "odf-s3-bucket.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
provisioner: openshift-storage.noobaa.io/obc
parameters:
  bucketclass: {{ default (include "odf-s3-bucket.bucketClassName" .) .Values.obcStorageClass.bucketClassName }}
reclaimPolicy: {{ .Values.obcStorageClass.reclaimPolicy }}
volumeBindingMode: {{ .Values.obcStorageClass.volumeBindingMode }}
{{- end }}
